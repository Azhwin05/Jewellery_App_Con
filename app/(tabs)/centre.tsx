// Placeholder screen for the centre FAB tab
// This tab is never navigated to — the FAB opens a bottom sheet instead
import { View } from 'react-native';
export default function CentreScreen() {
  return <View />;
}
